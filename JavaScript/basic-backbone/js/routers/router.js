/*global define*/
define([
    'jquery',
    'backbone',
    'collections/base-collection'
], function ($, Backbone, Base) {
    'use strict';

    var BasicRouter = Backbone.Router.extend({
    });

    return BasicRouter;
});
