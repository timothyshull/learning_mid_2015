describe("A basic Jasmine test suite", function() {
    var a;
    it("shows that Jasmine tests follow common JS function patterns", function() {
        a = true;
        expect(a).toBe(true);
    });
});