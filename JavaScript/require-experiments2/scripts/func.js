/*global define */
define(
    function() {
        "use strict";
        //return a function to define "foo/title".
        //It gets or sets the window title.
        (function testTitle() {
            return 3;
        }());
    }
);